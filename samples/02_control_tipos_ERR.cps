let a: integer = 2;
            if (a) { }                      // condición no booleana
            while (a) { a = a - 1; }        // no booleana
            for (let i: integer = 0; i + 1; i = i + 1) { }  // no booleana
